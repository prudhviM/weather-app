import React from 'react';

const DisplayTemp = props => {
    console.log(props.data);
    return (
        <span>
            {/* {props.data.temp} */}
        </span>
    )
};

export default DisplayTemp;